# This workflow is Dummy.
# DO Dispatching datapack branches!

name: PackSquash

on: workflow_dispatch

jobs:
  dummy:
    runs-on: ubuntu-24.04
    steps:
      - name: Dummy # Always fail
        run: |
          echo This workflow is Dummy.
          echo Retry Dispatching from datapack branches!
          exit 1
