# This workflow is Dummy.
# DO Dispatching datapack branches!

name: Trying PackSquash

on: workflow_dispatch

jobs:
  dummy:
    runs-on: ubuntu-24.04
    steps:
      - name: Dummy
        run: |
          'echo This workflow is Dummy.'
          'echo DO Dispatching datapack branches!'
